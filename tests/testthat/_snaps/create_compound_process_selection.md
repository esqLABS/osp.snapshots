# create_compound_process_selection validates strings

    Code
      create_compound_process_selection(name = "")
    Condition
      Error in `create_compound_process_selection()`:
      ! `name` must be a non-empty string

