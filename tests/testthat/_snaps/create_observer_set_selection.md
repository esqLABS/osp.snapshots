# create_observer_set_selection validates arguments

    Code
      create_observer_set_selection()
    Condition
      Error in `create_observer_set_selection()`:
      ! `name` must be a non-empty string

---

    Code
      create_observer_set_selection(name = "")
    Condition
      Error in `create_observer_set_selection()`:
      ! `name` must be a non-empty string

