% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/create_observer.R
\name{create_observer}
\alias{create_observer}
\title{Create a new observer}
\usage{
create_observer(
  name,
  type,
  dimension = NULL,
  formula = NULL,
  formula_references = NULL,
  container_criteria = NULL,
  molecule_list = NULL
)
}
\arguments{
\item{name}{Character. Name of the observer (required).}

\item{type}{Character. The observer type, one of \code{"Amount"} (an
\code{AmountObserverBuilder}) or \code{"Container"} (a
\code{ContainerObserverBuilder}). Any other value aborts (required).}

\item{dimension}{Character. The dimension of the observed quantity.
Omitted when \code{NULL}.}

\item{formula}{The observer's formula. Either a bare expression string
(which becomes the inner \code{Formula$Formula}), or a full
\code{ExplicitFormula} raw list used as the whole \code{Formula}. Omitted when
\code{NULL}.}

\item{formula_references}{List of formula references, each a
\code{\link[=create_formula_reference]{create_formula_reference()}} output or an equivalent raw
\verb{\{Alias, Path, Dimension\}} list. Written to the formula's
\code{References}; supplying these without \code{formula} yields a \code{Formula}
carrying only \code{References}. When \code{formula} is a full \code{ExplicitFormula}
list that already carries its own \code{References}, this argument
overrides them. Omitted when \code{NULL}.}

\item{container_criteria}{List of container criteria, each a
\code{\link[=create_descriptor_condition]{create_descriptor_condition()}} output or an equivalent raw
\verb{\{Tag, Type\}} list. Written to \code{ContainerCriteria}. Omitted when
\code{NULL}.}

\item{molecule_list}{A \code{\link[=create_molecule_list]{create_molecule_list()}} output or an equivalent
raw list. Written to \code{MoleculeList}. Omitted when \code{NULL}.}
}
\value{
An \link{Observer} object.
}
\description{
Create a single \link{Observer} from named arguments. This is a thin factory
around \code{Observer$new()} that builds the raw list shape for you and
composes with \code{\link[=create_observer_set]{create_observer_set()}} via
\code{create_observer_set(observers = list(...))}.

An observer is a simulation-time formula that computes a derived
quantity from the underlying model (for example an amount observer or a
container observer). It is not itself a building block; it lives inside
an \link{ObserverSet}.
}
\examples{
# Minimal amount observer
create_observer(name = "drug_amount", type = "Amount")

# Container observer with all sub-properties
obs <- create_observer(
  name = "brain_plasma_conc",
  type = "Container",
  dimension = "Concentration (molar)",
  formula = "Conc_Br",
  formula_references = list(
    create_formula_reference(
      "Conc_Br",
      "Organism|Brain|Plasma|Drug|Concentration"
    )
  ),
  container_criteria = list(
    create_descriptor_condition("Brain", "InContainer")
  ),
  molecule_list = create_molecule_list(for_all = FALSE, include = "Drug")
)

# Compose into an observer set
create_observer_set(name = "BrainPlasmaConcentration", observers = list(obs))
}
