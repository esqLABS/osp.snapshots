% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/resolve-simulation-refs.R
\name{get_default_alternative}
\alias{get_default_alternative}
\title{Get the default alternative of a compound property}
\usage{
get_default_alternative(alternatives)
}
\arguments{
\item{alternatives}{A compound physicochemical-property group, such as
\code{compound$solubility}, \code{compound$lipophilicity}, or any list of named
alternatives each carrying an optional \code{IsDefault} flag.}
}
\value{
The default alternative's name as a character string, or \code{NULL}
when the group has no resolvable default.
}
\description{
Returns the name of the default alternative in a compound
physicochemical-property group, for example the \code{Solubility},
\code{Lipophilicity}, or \code{FractionUnbound} field of a \link{Compound}. The default
is the alternative whose \code{IsDefault} flag is \code{TRUE} (an absent flag is
treated as \code{TRUE}, per the snapshot schema); when no alternative sets it,
the \code{"User defined"} alternative is used, and a group with no resolvable
default returns \code{NULL}.
}
\examples{
compound <- create_compound(
  name = "Drug X",
  solubility = list(
    solubility(9999, name = "Aqueous"),
    solubility(200, name = "FaSSIF", default = TRUE)
  )
)
get_default_alternative(compound$solubility)
}
