% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/create_schema_item.R
\name{create_schema_item}
\alias{create_schema_item}
\title{Create a new schema item}
\usage{
create_schema_item(
  name,
  application_type,
  formulation_key = NULL,
  target_organ = NULL,
  target_compartment = NULL,
  parameters = NULL,
  dose = NULL,
  dose_unit = "mg",
  start_time = NULL,
  start_time_unit = "h"
)
}
\arguments{
\item{name}{Character. Name of the schema item (required).}

\item{application_type}{Character. Application type for the schema
item (required). Must be one of the canonical PK-Sim application
types: \code{"Oral"}, \code{"Intravenous"}, \code{"IntravenousBolus"}, or
\code{"UserDefined"}.}

\item{formulation_key}{Character. Formulation key linking the schema
item to a formulation selection in the owning simulation.}

\item{target_organ}{Character. Target organ for the application. Only
valid when \code{application_type} is \code{"UserDefined"}.}

\item{target_compartment}{Character. Target compartment for the
application. Only valid when \code{application_type} is \code{"UserDefined"}.}

\item{parameters}{List of \link{Parameter} objects (created with
\code{\link[=create_parameter]{create_parameter()}}) or raw parameter lists. These become the
application-level parameters (dose, start time, ...). The promoted
\code{dose} and \code{start_time} arguments flow into the same \code{"InputDose"}
and \code{"Start time"} parameters, so supplying a setting both as a
promoted argument and as the matching \code{parameters} entry is an error.}

\item{dose}{Numeric scalar dose for the application, written as a
single \code{InputDose} parameter. \code{NULL} (default) emits no dose
parameter. The dose family (plain dose, per body weight, or per body
surface area) is selected by \code{dose_unit}.}

\item{dose_unit}{Character. Unit for \code{dose}, default \code{"mg"}. Must be a
dose-family unit (a Mass, Amount, dose-per-body-weight, or
dose-per-body-surface-area unit, for example \code{"mg"}, \code{"mg/kg"}, or
\code{"mg/m²"}). Consulted only when \code{dose} is supplied.}

\item{start_time}{Numeric scalar application start time, written as a
\code{"Start time"} parameter. \code{NULL} (default) emits no start-time
parameter. Zero and negative values are allowed.}

\item{start_time_unit}{Character. Unit for \code{start_time}, default \code{"h"},
validated against the \code{"Time"} dimension. Consulted only when
\code{start_time} is supplied.}
}
\value{
A \link{SchemaItem} object.
}
\description{
Create a \link{SchemaItem} for an Advanced \link{Protocol} from named arguments.
This is a thin factory around \code{SchemaItem$new()} that builds the raw
list shape for you.

A schema item is one application inside a \link{Schema} of an Advanced
Protocol. It carries an application type, an optional formulation key,
target organ and compartment, and the application-level parameters
(dose, start time, ...).
}
\examples{
# An oral schema item using the promoted dose and start-time arguments
item <- create_schema_item(
  name = "Item 1",
  application_type = "Oral",
  dose = 10,
  dose_unit = "mg",
  start_time = 0
)

# The same, authored through the free-form `parameters` escape hatch
item <- create_schema_item(
  name = "Item 1",
  application_type = "Oral",
  parameters = list(
    create_parameter(name = "Start time", value = 0, unit = "h"),
    create_parameter(name = "InputDose", value = 10, unit = "mg")
  )
)

# A user-defined schema item targeting the venous blood plasma
item <- create_schema_item(
  name = "User defined",
  application_type = "UserDefined",
  target_organ = "VenousBlood",
  target_compartment = "Plasma",
  dose = 5
)
}
