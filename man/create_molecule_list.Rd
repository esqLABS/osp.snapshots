% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/create_molecule_list.R
\name{create_molecule_list}
\alias{create_molecule_list}
\title{Create a molecule list for an observer}
\usage{
create_molecule_list(for_all = NULL, include = NULL, exclude = NULL)
}
\arguments{
\item{for_all}{Logical (length 1). When \code{TRUE}, the observer applies to
all molecules. Omitted when \code{NULL}.}

\item{include}{Character vector of molecule names to include. Always
serialises as a JSON array, even for a single name. Omitted when
\code{NULL}.}

\item{exclude}{Character vector of molecule names to exclude. Always
serialises as a JSON array, even for a single name. Omitted when
\code{NULL}.}
}
\value{
A named list with \code{ForAll}, \code{MoleculeNamesToInclude}, and
\code{MoleculeNamesToExclude} for the arguments supplied (an empty named
list when all are \code{NULL}).
}
\description{
Build a \code{MoleculeList} for an \link{Observer}, selecting which molecules the
observer applies to. This is a small builder that returns the raw named
list for \code{\link[=create_observer]{create_observer()}} or \code{observer$molecule_list}.
}
\examples{
create_molecule_list(for_all = FALSE, include = "Drug")

# Multiple names and an exclusion
create_molecule_list(include = c("Drug", "Metabolite"), exclude = "Water")
}
