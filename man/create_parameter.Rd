% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Parameter.R
\name{create_parameter}
\alias{create_parameter}
\title{Create a new parameter}
\usage{
create_parameter(
  name,
  value,
  path = NULL,
  unit = NULL,
  source = NULL,
  description = NULL,
  source_id = NULL,
  source_method = NULL,
  table_formula = NULL,
  table_points = NULL,
  x_name = NULL,
  y_name = NULL,
  x_unit = NULL,
  x_dimension = NULL,
  y_dimension = NULL
)
}
\arguments{
\item{name}{Character. Name of the parameter.}

\item{value}{Numeric. Value of the parameter.}

\item{path}{Character. Full container path of the parameter within its
parameter tree. Supply this to obtain a \link{LocalizedParameter} (used in
Individual, ExpressionProfile, and Simulation parameter sections).}

\item{unit}{Character. Unit of the parameter (optional).}

\item{source}{Character. Source of the value (optional).}

\item{description}{Character. Description of the value origin (optional).}

\item{source_id}{Integer. ID of the source (optional).}

\item{source_method}{Character. Value-origin determination method
(optional), mapped to \code{ValueOrigin.Method} in the emitted snapshot. One
of the \code{ValueOriginDeterminationMethodId} values: \code{"Undefined"},
\code{"Unknown"}, \code{"Assumption"}, \code{"ManualFit"}, \code{"ParameterIdentification"},
\code{"InVitro"}, \code{"InVivo"}, or \code{"Other"}.}

\item{table_formula}{List. Table formula data for table parameters
(optional).}

\item{table_points}{List. Points for table parameters, a list of x,y pairs
(optional).}

\item{x_name}{Character. Name of X axis for table parameters (optional).}

\item{y_name}{Character. Name of Y axis for table parameters (optional).}

\item{x_unit}{Character. Unit for X axis for table parameters (optional).}

\item{x_dimension}{Character. Dimension for X axis for table parameters
(optional).}

\item{y_dimension}{Character. Dimension for Y axis for table parameters
(optional).}
}
\value{
A \link{Parameter} object, or a \link{LocalizedParameter} when \code{path} is
supplied.
}
\description{
Create a new parameter with the specified properties.
All arguments except name and value are optional.

Returns a \link{LocalizedParameter} when a non-NULL \code{path} is supplied (i.e.
the parameter is identified by its position within a target's parameter
tree); otherwise returns a plain \link{Parameter}.
}
\section{Data shape}{
Plain \code{Parameter} objects carry the identifier in \code{data$Name}, matching
the JSON shape used by \code{Parameter} slots in \code{Compound}, \code{Formulation},
\code{Protocol}, and \code{Event} (per \code{snapshot-spec.md}). \code{LocalizedParameter}
objects carry the identifier in \code{data$Path}. The factory writes whichever
field matches the returned class so the raw \code{data} shape reflects the
kind of parameter unambiguously.
}

\examples{
# Create a basic parameter
param <- create_parameter(
  name = "Organism|Liver|Volume",
  value = 1.5
)

# Create a parameter with unit
param <- create_parameter(
  name = "Organism|Liver|Volume",
  value = 1.5,
  unit = "L"
)

# Create a parameter with value origin
param <- create_parameter(
  name = "Organism|Liver|Volume",
  value = 1.5,
  unit = "L",
  source = "Publication",
  description = "Reference XYZ",
  source_method = "ParameterIdentification"
)

# Create a localized parameter (path-bearing)
localized <- create_parameter(
  path = "Organism|Liver|Volume",
  value = 1.5,
  unit = "L"
)

# Create a table parameter
param <- create_parameter(
  name = "Fraction (dose)",
  value = 0.0,
  table_points = list(
    list(x = 0.0, y = 0.0),
    list(x = 1.0, y = 0.5),
    list(x = 2.0, y = 0.8),
    list(x = 4.0, y = 1.0)
  ),
  x_name = "Time",
  y_name = "Fraction (dose)",
  x_unit = "h",
  x_dimension = "Time",
  y_dimension = "Dimensionless"
)
}
