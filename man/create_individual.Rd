% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Individual.R
\name{create_individual}
\alias{create_individual}
\title{Create a new individual}
\usage{
create_individual(
  name = "New Individual",
  species = NULL,
  population = NULL,
  gender = NULL,
  age = NULL,
  age_unit = "year(s)",
  weight = NULL,
  weight_unit = "kg",
  height = NULL,
  height_unit = "cm",
  gestational_age = NULL,
  gestational_age_unit = "week(s)",
  calculation_methods = NULL,
  disease_state = NULL,
  disease_state_parameters = NULL,
  seed = NULL,
  expression_profiles = NULL,
  description = NULL,
  parameters = NULL
)
}
\arguments{
\item{name}{Character. Name of the individual}

\item{species}{Character. Species of the individual (must be valid ospsuite Species)}

\item{population}{Character. Population of the individual (must be valid ospsuite HumanPopulation)}

\item{gender}{Character. Gender of the individual (must be valid ospsuite Gender)}

\item{age}{Numeric. Age of the individual}

\item{age_unit}{Character. Unit for age (must be valid unit for "Age in years")}

\item{weight}{Numeric. Weight of the individual}

\item{weight_unit}{Character. Unit for weight (must be valid unit for "Mass")}

\item{height}{Numeric. Height of the individual}

\item{height_unit}{Character. Unit for height (must be valid unit for "Length")}

\item{gestational_age}{Numeric. Gestational age of the individual (for infant/preterm individuals)}

\item{gestational_age_unit}{Character. Unit for gestational age (must be valid unit for "Time")}

\item{calculation_methods}{Character vector. Calculation methods used for the individual}

\item{disease_state}{Character. Disease state of the individual (optional)}

\item{disease_state_parameters}{List. Parameters for disease state (optional)}

\item{seed}{Integer. Simulation seed (optional)}

\item{expression_profiles}{Character vector of expression-profile composite
names (\code{Molecule|Species|Category}) to attach to the individual. Default
\code{NULL} (no profiles).}

\item{description}{Character. Free-text description of the individual.
Default \code{NULL} (no description).}

\item{parameters}{List of localized parameters (each created with
\code{create_parameter(path = ...)}), or the equivalent raw list form the
loader consumes, applied as the individual's \code{Parameters} overrides. Each
entry must be path-bearing. Default \code{NULL} (no parameter overrides).}
}
\value{
An Individual object
}
\description{
Create a new individual with the specified properties.
All arguments are optional and will use default values if not provided.
}
\examples{
# Create a minimal individual
individual <- create_individual(name = "Test Individual")

# Create a complete individual
individual <- create_individual(
  name = "John Doe",
  species = "Human",
  population = "European_ICRP_2002",
  gender = "MALE",
  age = 30,
  weight = 70,
  height = 175
)

# Create an individual with calculation methods
individual <- create_individual(
  name = "Test Individual",
  calculation_methods = c("Method 1", "Method 2", "Method 3")
)

# Create an individual with disease state
individual <- create_individual(
  name = "Patient",
  disease_state = "CKD",
  disease_state_parameters = list(
    list(Name = "eGFR", Value = 45.0, Unit = "ml/min/1.73m²")
  )
)

# Create an individual referencing expression profiles with a localized
# parameter and a description
individual <- create_individual(
  name = "Subject 1",
  expression_profiles = c("CYP3A4|Human|Healthy", "P-gp|Human|Healthy"),
  description = "Reference healthy adult",
  parameters = list(
    create_parameter(
      path = "Organism|Liver|EHC continuous fraction",
      value = 1
    )
  )
)
}
