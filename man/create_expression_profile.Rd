% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/create_expression_profile.R
\name{create_expression_profile}
\alias{create_expression_profile}
\title{Create a new expression profile}
\usage{
create_expression_profile(
  molecule,
  species,
  category,
  type,
  localization = NULL,
  transport_type = NULL,
  ontogeny = NULL,
  parameters = NULL,
  expression = NULL,
  disease = NULL,
  description = NULL,
  reference_concentration = NULL,
  reference_concentration_unit = "µmol/l",
  half_life_liver = NULL,
  half_life_liver_unit = "h",
  half_life_intestine = NULL,
  half_life_intestine_unit = "h"
)
}
\arguments{
\item{molecule}{Character. Name of the molecule (for example
\code{"CYP3A4"}). Required.}

\item{species}{Character. Species (for example \code{"Human"}). Required.}

\item{category}{Character. Category disambiguator (for example
\code{"Healthy"} or \code{"Variability"}). Required.}

\item{type}{Character. Molecule type, typically one of \code{"Enzyme"},
\code{"Transporter"}, or \code{"OtherProtein"}. Required.}

\item{localization}{Character. Expression localization for proteins.}

\item{transport_type}{Character. Transporter type (for transporter
profiles), for example \code{"Efflux"} or \code{"Influx"}.}

\item{ontogeny}{Character or list. Ontogeny name, or a raw ontogeny
list. If a string is supplied it is wrapped as
\code{list(Name = ontogeny)}.}

\item{parameters}{List of \link{Parameter} objects (created with
\code{\link[=create_parameter]{create_parameter()}}) or raw parameter lists describing relative
expression and other per-organ values.}

\item{expression}{Per-organ relative expression, written to the
snapshot's \code{Expression} array. Supply a data frame (or tibble), one
row per organ/compartment, with a required \code{name} column
(organ/container name, for example \code{"Liver"}) and the optional
columns \code{value} (relative expression), \code{compartment} (for example
\code{"Intracellular"}), and \code{transport_direction} (for transporter
profiles). Missing (\code{NA}) or absent optional cells emit no key, so a
row with only \code{name} and \code{value} produces a container with just those
two fields. Row order is preserved and duplicate names are kept.
Alternatively supply a raw list of container lists (each a named list
with any of \code{Name}, \code{Value}, \code{CompartmentName}, \code{TransportDirection})
to pass through verbatim. \code{NULL} or an empty input writes nothing.}

\item{disease}{Disease state, written to the snapshot's \code{Disease}
object. Supply a named list with \code{name} (required, the
\code{DiseaseState} name) and an optional \code{parameters} list of \link{Parameter}
objects or raw parameter lists. \code{NULL} writes nothing.}

\item{description}{Character. Free-text description of the profile.}

\item{reference_concentration}{Numeric or \code{NULL}. Global molecule
reference concentration, written to the \code{Path}
\code{"{molecule}|Reference concentration"}. Validated as a single finite
numeric. \code{NULL} (default) emits nothing. Dimension
\code{"Concentration (molar)"}.}

\item{reference_concentration_unit}{Character. Unit for
\code{reference_concentration}, validated against the
\code{"Concentration (molar)"} dimension. Default \code{"µmol/l"}.
Consulted only when \code{reference_concentration} is supplied.}

\item{half_life_liver}{Numeric or \code{NULL}. Global molecule liver
half-life, written to the \code{Path} \code{"{molecule}|t1/2 (liver)"}.
Validated as a single finite numeric. \code{NULL} (default) emits nothing.
Dimension \code{"Time"}.}

\item{half_life_liver_unit}{Character. Unit for \code{half_life_liver},
validated against the \code{"Time"} dimension. Default \code{"h"}. Consulted
only when \code{half_life_liver} is supplied.}

\item{half_life_intestine}{Numeric or \code{NULL}. Global molecule intestine
half-life, written to the \code{Path} \code{"{molecule}|t1/2 (intestine)"}.
Validated as a single finite numeric. \code{NULL} (default) emits nothing.
Dimension \code{"Time"}.}

\item{half_life_intestine_unit}{Character. Unit for
\code{half_life_intestine}, validated against the \code{"Time"} dimension.
Default \code{"h"}. Consulted only when \code{half_life_intestine} is supplied.}
}
\value{
An \link{ExpressionProfile} object.
}
\description{
Create an \link{ExpressionProfile} building block from named arguments.
This is a thin factory around \code{ExpressionProfile$new()} that builds
the raw list shape for you.

An \link{ExpressionProfile}'s identity is the composite
\code{Molecule|Species|Category}, so all three of \code{molecule}, \code{species},
and \code{category} are required.

The \code{reference_concentration}, \code{half_life_liver}, and
\code{half_life_intestine} arguments promote the three fixed global
molecule-properties scalars as a convenience over hand-authoring them
through \code{parameters}. A promoted argument and the equivalent
\code{parameters} entry produce identical snapshot JSON.
}
\examples{
# Create a minimal enzyme expression profile
profile <- create_expression_profile(
  molecule = "CYP3A4",
  species = "Human",
  category = "Healthy",
  type = "Enzyme"
)

# Create a transporter expression profile with ontogeny
profile <- create_expression_profile(
  molecule = "P-gp",
  species = "Human",
  category = "Healthy",
  type = "Transporter",
  transport_type = "Efflux",
  ontogeny = "P-gp"
)

# Create an enzyme profile with per-organ relative expression
profile <- create_expression_profile(
  molecule = "CYP3A4",
  species = "Human",
  category = "Healthy",
  type = "Enzyme",
  expression = data.frame(
    name = c("Liver", "Kidney"),
    value = c(1, 0.5)
  )
)

# Set the global reference concentration directly
profile <- create_expression_profile(
  molecule = "CYP3A4",
  species = "Human",
  category = "Healthy",
  type = "Enzyme",
  reference_concentration = 4.32
)
}
