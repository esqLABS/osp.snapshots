% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/value_specs.R
\name{solubility}
\alias{solubility}
\title{Solubility value object}
\usage{
solubility(
  value = NULL,
  unit = "mg/l",
  reference_pH = NULL,
  gain_per_charge = NULL,
  table = NULL,
  name = "User defined",
  default = FALSE
)
}
\arguments{
\item{value}{Numeric scalar. Solubility-at-reference-pH value for the
scalar form. Leave \code{NULL} for the table form.}

\item{unit}{Character. Unit for the solubility value (scalar form) and for
the table Y values, validated against dimension
\code{"Concentration (mass)"}. Defaults to \code{"mg/l"}.}

\item{reference_pH}{Numeric scalar. Reference pH added to the scalar
\code{Solubility} alternative. Scalar form only.}

\item{gain_per_charge}{Numeric scalar. Optional
\verb{Solubility gain per charge} parameter added to the scalar \code{Solubility}
alternative. Scalar form only.}

\item{table}{Two-column data frame giving table-based solubility: the first
column is pH and the second is the solubility value. When supplied, the
table form is built and the scalar-form arguments must be left unset.}

\item{name}{Character. \code{Name} of the created alternative (scalar or table).
Defaults to \code{"User defined"}.}

\item{default}{Logical. Whether this alternative is the group's default
when it appears in a list of alternatives passed to the matching
\code{\link[=create_compound]{create_compound()}} argument or \code{Compound} field. Defaults to \code{FALSE}.
Ignored for a single (non-list) value object, which is always the
default. When a list has no element marked \code{default = TRUE}, the first
element is the default (unchanged behaviour); marking two or more
elements \code{default = TRUE} in the same list is an error.}
}
\value{
A \code{solubility_spec} object to pass to \code{\link[=create_compound]{create_compound()}}.
}
\description{
Build a solubility value object for \code{\link[=create_compound]{create_compound()}}'s \code{solubility}
argument (or the writable \code{Compound$solubility} field). Expresses either
the scalar form (a single solubility value at a reference pH, optionally
with a solubility gain per charge) or the mutually exclusive table form (a
pH/value table).

The scalar-form arguments (\code{value}, \code{reference_pH}, \code{gain_per_charge}) and
\code{table} are mutually exclusive: supplying \code{table} together with any of them
is an error. Unlike the previous \code{create_compound()} behaviour, where
\code{reference_pH} / \code{gain_per_charge} were silently ignored on the table path,
this helper rejects the combination so the intent is unambiguous.
}
\examples{
# Scalar form with reference pH and gain per charge
solubility(9999, reference_pH = 7, gain_per_charge = 1000)

# Table form (first column pH, second column value)
solubility(table = data.frame(pH = c(3, 6, 6.8), value = c(5000, 3000, 90)))

# Several alternatives, the second marked as the default
create_compound(
  name = "Drug X",
  solubility = list(
    solubility(9999, name = "Aqueous"),
    solubility(200, name = "FaSSIF", default = TRUE)
  )
)
}
\seealso{
\code{\link[=create_compound]{create_compound()}}

Other value-object helpers:
\code{\link[=age]{age()}},
\code{\link[=error]{error()}},
\code{\link[=fraction_unbound]{fraction_unbound()}},
\code{\link[=gestational_age]{gestational_age()}},
\code{\link[=height]{height()}},
\code{\link[=intestinal_permeability]{intestinal_permeability()}},
\code{\link[=lipophilicity]{lipophilicity()}},
\code{\link[=permeability]{permeability()}},
\code{\link[=time]{time()}},
\code{\link[=values]{values()}},
\code{\link[=weight]{weight()}}
}
\concept{value-object helpers}
