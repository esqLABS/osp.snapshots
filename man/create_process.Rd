% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/create_process.R
\name{create_process}
\alias{create_process}
\title{Create a new compound process}
\usage{
create_process(
  internal_name,
  data_source,
  molecule = NULL,
  metabolite = NULL,
  species = NULL,
  parameters = NULL
)
}
\arguments{
\item{internal_name}{Character. The PK-Sim \code{InternalName} for the
process template (e.g. \code{"SpecificBinding"},
\code{"MetabolizationSpecific_MM"}, \code{"GlomerularFiltration"}). Required.}

\item{data_source}{Character. The \code{DataSource} string identifying the
process. Required.}

\item{molecule}{Character. Optional molecule name for partial processes
(e.g. the enzyme catalysing the reaction).}

\item{metabolite}{Character. Optional metabolite name for enzymatic
processes.}

\item{species}{Character. Optional species name for species-dependent
processes.}

\item{parameters}{List of \link{Parameter} objects (created with
\code{\link[=create_parameter]{create_parameter()}}) or raw parameter lists to attach to the process.}
}
\value{
A \link{Process} object.
}
\description{
Create a \link{Process} (a PK-Sim \code{CompoundProcess}) from named arguments.
This is a thin factory around \code{Process$new()} that builds the raw list
shape for you.

For richer process structures (such as parameters with full value
origins), build the raw list directly or load a snapshot and copy the
relevant entry.
}
\examples{
# Minimal protein-binding process
p <- create_process(
  internal_name = "SpecificBinding",
  data_source = "Publication X",
  molecule = "Albumin"
)

# Metabolizing enzyme with a parameter
p <- create_process(
  internal_name = "MetabolizationSpecific_MM",
  data_source = "Optimized",
  molecule = "CYP3A4",
  metabolite = "Hydroxy-Drug",
  parameters = list(
    create_parameter(name = "Km", value = 1.2, unit = "µmol/l")
  )
)
}
