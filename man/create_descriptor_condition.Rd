% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/create_descriptor_condition.R
\name{create_descriptor_condition}
\alias{create_descriptor_condition}
\title{Create a container criterion for an observer}
\usage{
create_descriptor_condition(tag, type = NULL)
}
\arguments{
\item{tag}{Character. The container tag to match (required).}

\item{type}{Character. The match type. This is an open string, not a
closed enum: PK-Sim writes values such as \code{"InContainer"},
\code{"NotInContainer"}, \code{"InParent"}, or \code{"MatchTag"}, and any of these
(or another string PK-Sim emits) is preserved verbatim. Omitted when
\code{NULL}.}
}
\value{
A named list with \code{Tag} and, when supplied, \code{Type}.
}
\description{
Build a single \code{DescriptorCondition} entry for an \link{Observer}'s
\code{ContainerCriteria}. A container criterion matches the containers an
observer applies to by a \code{Tag} and an optional match \code{Type}. This is a
small builder that returns the raw named list; assemble several of them
into a list for \code{\link[=create_observer]{create_observer()}} or \code{observer$container_criteria}.
}
\examples{
create_descriptor_condition("Brain", "InContainer")

# The match type is optional
create_descriptor_condition("Brain")
}
