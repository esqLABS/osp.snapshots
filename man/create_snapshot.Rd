% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/create_snapshot.R
\name{create_snapshot}
\alias{create_snapshot}
\title{Create an empty snapshot}
\usage{
create_snapshot(name = NULL, description = NULL)
}
\arguments{
\item{name}{Character. Optional snapshot name. When supplied, it is
written to the snapshot's \code{Name}; when \code{NULL}, no \code{Name} is set. An
empty string \code{""} is written verbatim rather than treated as absent.}

\item{description}{Character. Optional snapshot description. Follows the
same \code{NULL}-versus-supplied and empty-string semantics as \code{name}.}
}
\value{
A \link{Snapshot} object.
}
\description{
Create an in-memory \link{Snapshot} from scratch, carrying the current
supported PK-Sim version and no building blocks. This is the
snapshot-level constructor that pairs with \code{\link[=load_snapshot]{load_snapshot()}} and
\code{\link[=export_snapshot]{export_snapshot()}}: rather than loading an existing project, it starts
an empty one you can then populate.

The result touches no files and has no path. Mutate it with the
\verb{add_*()} verbs (for example \code{\link[=add_compound]{add_compound()}}) and serialize it with
\code{\link[=export_snapshot]{export_snapshot()}}.
}
\examples{
# An empty snapshot
snapshot <- create_snapshot()

# A named and described snapshot
snapshot <- create_snapshot(name = "My Project", description = "Notes")

# Populate it with a building block
snapshot <- add_compound(create_snapshot(), create_compound(name = "Drug X"))
}
\seealso{
\code{\link[=load_snapshot]{load_snapshot()}}, \code{\link[=export_snapshot]{export_snapshot()}}
}
