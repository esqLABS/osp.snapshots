% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Compound.R
\name{Compound}
\alias{Compound}
\title{Compound class for OSP snapshot compounds}
\description{
An R6 class that represents a compound in an OSP snapshot.
This class provides methods to access different properties of a compound
and display a summary of its information.

Compound processes are exposed via \verb{$processes}, a flat named list of
\link{Process} objects. The per-category tibble accessors
(\verb{$protein_binding_partners}, \verb{$metabolizing_enzymes},
\verb{$hepatic_clearance}, \verb{$transporter_proteins}, \verb{$renal_clearance},
\verb{$biliary_clearance}, \verb{$inhibition}, \verb{$induction}) are
\code{\link[lifecycle:deprecate_soft]{lifecycle::deprecate_soft()}}-warned in favour of \verb{$processes} and the
long-form \code{processes} tibble returned by \code{\link[=get_compounds_dfs]{get_compounds_dfs()}}.
}
\section{Active bindings}{
  \if{html}{\out{<div class="r6-active-bindings">}}
  \describe{
    \item{\code{data}}{The raw data of the compound (read-only). Refreshed from
the embedded \link{CalculationMethods} and the cached \link{Process}
objects so that mutations flow back to the export payload.}

    \item{\code{name}}{The name of the compound}

    \item{\code{is_small_molecule}}{Whether the compound is a small molecule}

    \item{\code{plasma_protein_binding_partner}}{The plasma protein binding partner of the compound}

    \item{\code{molecular_weight}}{The molecular weight of the compound}

    \item{\code{molecular_weight_unit}}{The unit of the molecular weight}

    \item{\code{lipophilicity}}{The lipophilicity data of the compound. Writable:
assign a \code{\link[=lipophilicity]{lipophilicity()}} object, a numeric scalar to create a
single default \code{Lipophilicity} alternative (parameter
\code{"Lipophilicity"}, unit \code{"Log Units"}), a raw alternative list to
set the array verbatim (the escape hatch for multiple, named, or
species-specific alternatives), or \code{NULL} to clear the property.}

    \item{\code{fraction_unbound}}{The fraction unbound data of the compound.
Writable: assign a \code{\link[=fraction_unbound]{fraction_unbound()}} object, a numeric scalar to
create a single default \code{FractionUnbound} alternative (parameter
\code{"Fraction unbound (plasma, reference value)"}, no unit), a raw
alternative list to set the array verbatim, or \code{NULL} to clear the
property.}

    \item{\code{solubility}}{The solubility data of the compound. Writable:
assign a \code{\link[=solubility]{solubility()}} object to express reference pH, gain per
charge, or a pH/value table, a numeric scalar to create a single
default \code{Solubility} alternative (parameter
\code{"Solubility at reference pH"}, unit \code{"mg/l"}), a raw alternative
list to set the array verbatim, or \code{NULL} to clear the property.
The numeric-scalar form cannot express reference pH, gain per
charge, or table solubility; use a \code{\link[=solubility]{solubility()}} object or a raw
alternative list for those.}

    \item{\code{intestinal_permeability}}{The intestinal permeability data of the
compound. Writable: assign an \code{\link[=intestinal_permeability]{intestinal_permeability()}} object, a
numeric scalar to create a single default \code{IntestinalPermeability}
alternative (parameter
\code{"Specific intestinal permeability (transcellular)"}, unit
\code{"cm/min"}), a raw alternative list to set the array verbatim, or
\code{NULL} to clear the property.}

    \item{\code{permeability}}{The permeability data of the compound. Writable:
assign a \code{\link[=permeability]{permeability()}} object, a numeric scalar to create a
single default \code{Permeability} alternative (parameter
\code{"Permeability"}, unit \code{"cm/min"}), a raw alternative list to set
the array verbatim, or \code{NULL} to clear the property.}

    \item{\code{pka_types}}{The pKa types of the compound. Writable: assign a
list of \code{list(type =, value =)} entries (the \code{create_compound()}
\code{pKa} shape, converted to \code{list(Type =, Pka =)}), a raw \code{PkaType[]}
list (entries carrying \code{Type}/\code{Pka}, set verbatim), or \code{NULL} /
\code{list()} to clear the pKa types.}

    \item{\code{processes}}{A flat named list of \link{Process} objects, one per
entry in the compound's \code{Processes} array. Duplicate names are
disambiguated with a numeric suffix (\verb{_1}, \verb{_2}, ...). The list is
built once at construction so that state changes made on a
\link{Process} persist across accesses. Writable: assign a list of
\link{Process} objects and/or raw process lists to rebuild the processes
(duplicate names are disambiguated as at construction), or \code{NULL} /
\code{list()} to clear them.}

    \item{\code{calculation_methods}}{A \link{CalculationMethods} object holding the
compound's calculation methods.}

    \item{\code{parameters}}{The additional parameters of the compound (excluding molecular weight)}

    \item{\code{protein_binding_partners}}{Deprecated. Filter
\code{\link[=get_compounds_dfs]{get_compounds_dfs()}}\verb{$processes} on \code{category == "protein_binding_partners"}, or iterate \code{self$processes} and check
\code{process$category}.}

    \item{\code{metabolizing_enzymes}}{Deprecated. See \verb{$protein_binding_partners}.}

    \item{\code{hepatic_clearance}}{Deprecated. See \verb{$protein_binding_partners}.}

    \item{\code{transporter_proteins}}{Deprecated. See \verb{$protein_binding_partners}.}

    \item{\code{renal_clearance}}{Deprecated. See \verb{$protein_binding_partners}.}

    \item{\code{biliary_clearance}}{Deprecated. See \verb{$protein_binding_partners}.}

    \item{\code{inhibition}}{Deprecated. See \verb{$protein_binding_partners}.}

    \item{\code{induction}}{Deprecated. See \verb{$protein_binding_partners}.}
  }
  \if{html}{\out{</div>}}
}
\section{Methods}{
\subsection{Public methods}{
  \itemize{
    \item \href{#method-Compound-initialize}{\code{Compound$new()}}
    \item \href{#method-Compound-print}{\code{Compound$print()}}
    \item \href{#method-Compound-to_df}{\code{Compound$to_df()}}
    \item \href{#method-Compound-clone}{\code{Compound$clone()}}
  }
}
\if{html}{\out{<hr>}}
\if{html}{\out{<a id="method-Compound-initialize"></a>}}
\if{latex}{\out{\hypertarget{method-Compound-initialize}{}}}
\subsection{\code{Compound$new()}}{
  Create a new Compound object
  \subsection{Usage}{
    \if{html}{\out{<div class="r">}}
    \preformatted{Compound$new(data)}
    \if{html}{\out{</div>}}
  }
  \subsection{Arguments}{
    \if{html}{\out{<div class="arguments">}}
    \describe{
      \item{\code{data}}{Raw compound data from a snapshot}
    }
    \if{html}{\out{</div>}}
  }
  \subsection{Returns}{
    A new Compound object
  }
}

\if{html}{\out{<hr>}}
\if{html}{\out{<a id="method-Compound-print"></a>}}
\if{latex}{\out{\hypertarget{method-Compound-print}{}}}
\subsection{\code{Compound$print()}}{
  Print a summary of the compound including its properties and parameters.
  \subsection{Usage}{
    \if{html}{\out{<div class="r">}}
    \preformatted{Compound$print(...)}
    \if{html}{\out{</div>}}
  }
  \subsection{Arguments}{
    \if{html}{\out{<div class="arguments">}}
    \describe{
      \item{\code{...}}{Additional arguments passed to print methods}
    }
    \if{html}{\out{</div>}}
  }
  \subsection{Returns}{
    Invisibly returns the Compound object for method chaining
  }
}

\if{html}{\out{<hr>}}
\if{html}{\out{<a id="method-Compound-to_df"></a>}}
\if{latex}{\out{\hypertarget{method-Compound-to_df}{}}}
\subsection{\code{Compound$to_df()}}{
  Convert this compound's physicochemical properties and process
parameters to a single long-form tibble (legacy shape).

Used by \code{\link[=get_compounds_dfs]{get_compounds_dfs()}} to assemble the compound-wide
\code{properties} tibble. The process-derived rows produced here are
\code{\link[lifecycle:deprecate_soft]{lifecycle::deprecate_soft()}}-warned at the
\code{\link[=get_compounds_dfs]{get_compounds_dfs()}} entry point; prefer the long-form
\code{processes} tibble returned alongside.
  \subsection{Usage}{
    \if{html}{\out{<div class="r">}}
    \preformatted{Compound$to_df()}
    \if{html}{\out{</div>}}
  }
  \subsection{Returns}{
    A tibble with columns \code{compound}, \code{category}, \code{type},
\code{parameter}, \code{value}, \code{unit}, \code{data_source}, \code{source}.
  }
}

\if{html}{\out{<hr>}}
\if{html}{\out{<a id="method-Compound-clone"></a>}}
\if{latex}{\out{\hypertarget{method-Compound-clone}{}}}
\subsection{\code{Compound$clone()}}{
  The objects of this class are cloneable with this method.
  \subsection{Usage}{
    \if{html}{\out{<div class="r">}}
    \preformatted{Compound$clone(deep = FALSE)}
    \if{html}{\out{</div>}}
  }
  \subsection{Arguments}{
    \if{html}{\out{<div class="arguments">}}
    \describe{
      \item{\code{deep}}{Whether to make a deep clone.}
    }
    \if{html}{\out{</div>}}
  }
}

}
