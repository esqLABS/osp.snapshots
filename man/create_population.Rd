% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Population.R
\name{create_population}
\alias{create_population}
\title{Create a new population}
\usage{
create_population(
  name,
  number_of_individuals = 100,
  proportion_of_females = 50,
  age_min = 18,
  age_max = 65,
  age_unit = "year(s)",
  weight_min = NULL,
  weight_max = NULL,
  weight_unit = "kg",
  height_min = NULL,
  height_max = NULL,
  height_unit = "cm",
  seed = NULL
)
}
\arguments{
\item{name}{Character. Name of the population}

\item{number_of_individuals}{Numeric. Number of individuals in the population}

\item{proportion_of_females}{Numeric. Percentage of females in the population (0-100)}

\item{age_min}{Numeric. Minimum age for the population}

\item{age_max}{Numeric. Maximum age for the population}

\item{age_unit}{Character. Unit for age (must be valid unit for "Age in years")}

\item{weight_min}{Numeric. Minimum weight for the population (optional)}

\item{weight_max}{Numeric. Maximum weight for the population (optional)}

\item{weight_unit}{Character. Unit for weight (must be valid unit for "Mass")}

\item{height_min}{Numeric. Minimum height for the population (optional)}

\item{height_max}{Numeric. Maximum height for the population (optional)}

\item{height_unit}{Character. Unit for height (must be valid unit for "Length")}

\item{seed}{Integer. Simulation seed (optional)}
}
\value{
A Population object
}
\description{
Create a new population with the specified properties.
Most arguments are optional and will use default values if not provided.
Note that this function doesn't support creating advanced parameters or
defining template individuals manually. Template individuals are loaded
directly from snapshot files.
}
\examples{
\dontrun{
# Create a minimal population
population <- create_population(
  name = "Test Population",
  number_of_individuals = 100,
  proportion_of_females = 50,
  age_min = 18,
  age_max = 65
)

# Create a more detailed population
population <- create_population(
  name = "Detailed Population",
  number_of_individuals = 50,
  proportion_of_females = 60,
  age_min = 30,
  age_max = 60,
  age_unit = "year(s)",
  weight_min = 60,
  weight_max = 90,
  weight_unit = "kg",
  height_min = 160,
  height_max = 180,
  height_unit = "cm"
)
}
}
