% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/use-claude-code.R
\name{use_claude_code}
\alias{use_claude_code}
\title{Scaffold a companion skill for AI coding agents}
\usage{
use_claude_code(path = ".", overwrite = FALSE)
}
\arguments{
\item{path}{Target project root. Defaults to the current working directory
(\code{"."}). The skill is written under \code{path} at
\code{.claude/skills/osp-snapshots/SKILL.md}, creating any missing parent
directories.}

\item{overwrite}{Single logical. When \code{TRUE}, an existing skill file is
replaced and re-stamped without prompting. When \code{FALSE} (the default) and
the file exists, an interactive session asks for confirmation and a
non-interactive session aborts.}
}
\value{
The destination \code{SKILL.md} path, invisibly. Returned in every case,
including when an interactive user declines to overwrite an existing file.
}
\description{
Copies the bundled osp.snapshots companion skill into a project so an AI
coding agent (Claude Code or similar) can read it. The skill is written to
\verb{<path>/.claude/skills/osp-snapshots/SKILL.md} and teaches an agent to
consume the osp.snapshots public API and avoid its known gotchas. The
written file is stamped with the installed osp.snapshots version in its
\code{x-osp-snapshots-version} frontmatter field, so an outdated copy is
detectable from version control.

If the destination already exists and \code{overwrite} is \code{FALSE}, the behavior
depends on the session: an interactive session asks for confirmation, while
a non-interactive session (\verb{R CMD check}, CI, \code{Rscript}) aborts and asks you
to pass \code{overwrite = TRUE}. This keeps automated runs deterministic.
}
\examples{
\dontrun{
# Scaffold the skill into the current project.
use_claude_code()

# Refresh an existing skill after upgrading osp.snapshots.
use_claude_code(overwrite = TRUE)
}

}
