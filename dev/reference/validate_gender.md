# Validate that a gender is valid

Check if a gender is valid using ospsuite Gender enum. Throws an error
if the gender is invalid.

## Usage

``` r
validate_gender(gender)
```

## Arguments

- gender:

  Gender value to validate

## Value

TRUE if valid, throws an error if invalid
