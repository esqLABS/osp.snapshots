# Validate that an object is a Snapshot

Check if an object is a Snapshot instance. Throws an error if the object
is not a Snapshot.

## Usage

``` r
validate_snapshot(snapshot)
```

## Arguments

- snapshot:

  Object to validate

## Value

TRUE if valid, throws an error if invalid
