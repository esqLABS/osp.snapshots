# Validate that a species is valid

Check if a species is valid using ospsuite Species enum. Throws an error
if the species is invalid.

## Usage

``` r
validate_species(species)
```

## Arguments

- species:

  Species value to validate

## Value

TRUE if valid, throws an error if invalid
